---
name: mesh-split-fill
description: Split 3D meshes into clean semantic parts, repair or cap openings with smooth or complementary surfaces, and document the real construction process using Blender on Windows or macOS. Use for model disassembly, seam repair, mesh hole filling, or reusable mesh preparation workflows.
---

# 三维拆件与补洞

按用途拆分网格，保留开口、补平、圆润封口或建立互补面。适用于角色、服饰、机械外壳和扫描网格，不假定部件名称、数量、轴向或尺寸。这是代理执行的工作流及工具包，**不是对任意网格保证一键成功的语义分割器**。

## 先固定任务范围

1. 找到本技能目录（下文记为 `SKILL_DIR`），按 [部署与运行](references/deployment.md) 运行 `scripts/runtime.py doctor`。检测 Blender 和资源；CPU 是基础路径，无须独显。不自动下载模型、插件或改变共享环境。全部产物写入独立任务目录，源文件只读。
2. 运行 `scripts/job.py init --input MODEL --job-dir JOB`，再通过 Blender 执行 `scripts/blender_inspect.py`。记录实际对象、面组、裸边、原始瑕疵、材料、变换与尺寸；导入时不自动修理基线。
3. **先写完整清单，再拆件。** 填写 `JOB/job.json`：主体、左右件、背面附件、小装饰，以及需要保持相连的结构。每件附用户要求或视检证据，不把导入的对象数量当成目标数量。左右按模型轴向或视图证据判断，不能按画面左右命名。
4. 同时列出全部接缝、补面用途、互补关系、可修改过渡带、保留眼孔/通道及尺寸约束。“要求闭合”不代表封掉设计开口。缺少边界意图时只询问缺失项，同时继续独立检查；用户已说明的内容直接采用。
5. 若要求过程图，**此时固定案例、视角、版式、步骤和文件格式**，写入 `storyboard`；未要求则保持关闭。案例按困难几何类型选取，不固定为头发/耳朵。默认建议 1–3 例、每例 4–6 步，先做低分辨率布局预览；服从用户参考图与指定结构。
6. 范围完整后设置 `inventory.complete=true`，解决 `unresolved`，运行 `job.py freeze --job-dir JOB`。这是内部契约锁定，**不需要用户再次批准已授权工作**。需求变化时更新契约、重新冻结，只重做受影响的产物。

字段、缓存和候选记录见 [任务契约](references/workflow.md)。不要一次载入全部旧脚本和历史对话。

## 拆分与补面共用接缝设计

- 选最难的代表性接缝做端到端样件：拆分→补片→过渡带→导出重读→局部验收。通过后批量处理同类接缝，不同类型各验证一例。复杂处理前阅读 [几何方法](references/geometry.md)。
- 对象、材料和面标签仅作初选，结合曲率、凹槽、连通关系及隔离视图校正语义。`scripts/mesh_geometry.py` 提供显式三角面标签拆件和共享补面反绕序基础函数；内部切线、曲面求解与严格相交检查由任务适配器完成，不能把基础函数当成完整修复器。
- **先整理接缝再生补面。** 检查分叉、自交、短边、锯齿及内外壁。决定局部重划分带后，固定唯一接缝 ID、共享边界点序和来源映射；不要让相邻两件各自平滑后再对齐。
- 补片和过渡带一起验收，只固定远处表面。原面必须不变时，沿原三角面细分或重划分，不能暗中移动原顶点、整体平滑或重网格。必要变化与要求冲突时说明具体位置与最小可选方案。
- 互补面使用同一份补片数据，另一件反转绕序；明确要求加工间隙才加入偏移，并区分零间隙几何互补与实际可制造配合。轮廓不一致时先设计共同切面，不能用反转法向掩盖未吻合边界。
- 容差相对局部尺度定义，再结合真实单位解释；未知单位不称毫米。几何平滑不能由平滑着色代替；未经量化验证不声称严格 G1/G2 连续。

## 有限返修与验收

每次候选用 `job.py record` 记录接缝、策略、输入/脚本哈希、修改范围、质量指标、耗时和产物。同一接缝同一策略默认最多两次失败，之后改变几何分类或升级诊断，不能仅微调参数无限试跑。这是返修控制，不是停止整个任务。

从便宜到昂贵检查：

1. 部件清单完整、原面恰归一件；新切分保留重心来源。坐标、尺寸和保护区满足契约，无误丢面或重复面。
2. 开口与保留洞匹配，检查边流形、顶点面扇、绕序、退化面；裸边为零不能证明成功。
3. 检查补片、重建带和邻近原壳的反折、自交、穿插、共面覆盖；共享点/边的三角形也要检查正常接触之外的交叠。`not_checked` 不能报通过。
4. 检查切线变化、曲率趋势、厚度和互补误差；逐件隔离看外侧、背面、内壁及接缝。计算通过而语义不干净仍不合格。
5. 关键交付节点执行全件审查，另存后重读，核查实际存储精度下的退化、面数、单位和变换。与基线区分原有瑕疵和新增问题。

内置 `blender_inspect.py` 是**拓扑预检，不是严格自交验收器**。复杂交叠需实现或接入严格检查器；检查缺失时仅交付明确标记的候选，不宣称封闭无穿插。保留原材料、UV和面组；无法保留的属性写入候选限制，不静默丢弃。

## 过程图与交付

- 按冻结的案例边做边保存真实中间网格、参数和渲染，每步说明“改了哪里、为什么”。不把最终网格冒充中间步骤，不用 AI 生图替代几何证据。
- `scripts/storyboard.py --job-dir JOB --evidence evidence.json --output process.html` 生成固定版式、自包含可打印页面。它不冒充 PNG；若要求 PNG/PDF，用目标设备可用的浏览器/渲染器导出并检查文字与裁切，标明实际格式。
- 交付独立部件、原位装配工程、来源映射、验收摘要及要求的过程图。爆炸图仅改变展示副本的位置。
- 分开统计建模、验收和过程图。区分主任务运行时间、并行代理时间、CPU/GPU时间与 token；缺少遥测就报未知。

## 跨设备协作与成本

有子代理时并行安排独立部件、只读调查与审查，通常最多两名子代理，同一文件仅一位写入者。服从项目角色路由，不绑定模型名或客户端；无子代理时顺序执行同一流程。

交接完整清单、局部输入、失败证据和最新摘要，避免复制长历史。缓存键包含输入、冻结契约、脚本、参数和运行时版本；全部匹配且产物哈希正确才复用。不得复用另一模型的面索引、单位长度或设备路径。
